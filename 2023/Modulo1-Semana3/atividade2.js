// Ex 2 Crie uma função nomeada que recebe um array de elementos, imprima cada um dos elementos e em seguida retorne a quantidade de elementos no array (seu tamanho).

let arrayDeElementos = ["elem1", "elem2", "elem3", "elem4"]

function recebeElementos(arrayDeElementos) {
    for (let elementos of arrayDeElementos) {
        return elementos
    }
}

recebeElementos((["dsadsa", "dsad", "dsadasdasda"]));